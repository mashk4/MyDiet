<h1>
  Your monthly report from <%= (Date.today - 1).all_month.first.strftime('%d-%m-%Y') %>
  to <%= (Date.today - 1).all_month.last.strftime('%d-%m-%Y') %>
</h1>

<div>
  <p>Hello, <%= @user.email.split('@')[0] if @user.name.nil? || @user.name %>!</p>
  Here are the statistics for the last month of what you have eaten:
</div>

<div class="card-body mt-3">
    <%= render 'table', object: @eaten_products %>
</div>
